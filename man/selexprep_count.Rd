% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R
\name{selexprep_count}
\alias{selexprep_count}
\title{Count unique SELEX sequences}
\usage{
selexprep_count(sequences)
}
\arguments{
\item{sequences}{A character vector or a \code{Biostrings::XStringSet} containing
the primer-stripped sequences from one round.}
}
\value{
A \code{S4Vectors::DataFrame} with \code{sequence}, \code{reads}, \code{rank}, and \code{rpm}
columns, ordered by decreasing read count.
}
\description{
Counts the observed sequences in one primer-stripped SELEX round. The
result is a \code{S4Vectors::DataFrame} compatible with the stable Python output
schema: \code{sequence}, \code{reads}, \code{rank}, and \code{rpm}. The sequence column is a
\code{Biostrings::BStringSet} so that both DNA and RNA alphabets can be carried
without lossy coercion.
}
\details{
Ties in abundance are ordered lexicographically by sequence. The original
Python implementation does not define a tie-breaker; making it explicit in
R provides reproducible ranks across platforms.
}
\examples{
selexprep_count(c('ACGT', 'ACGT', 'GGGG'))
}
