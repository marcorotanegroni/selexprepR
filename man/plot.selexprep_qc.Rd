% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-plots.R
\name{plot.selexprep_qc}
\alias{plot.selexprep_qc}
\title{Plot SELEX quality-control diagnostics}
\usage{
\method{plot}{selexprep_qc}(
  x,
  y = NULL,
  which = c("retention", "primer_match", "length", "diversity"),
  ...
)
}
\arguments{
\item{x}{A \code{selexprep_qc} object returned by \code{selexprep_qc()}.}

\item{y}{Optional \code{SummarizedExperiment} used to compute \code{x}. It supplies
count distributions and extraction provenance.}

\item{which}{One or more diagnostic groups: \code{'retention'},
\code{'primer_match'}, \code{'length'}, or \code{'diversity'}.}

\item{...}{Named graphical parameters passed to \code{\link[graphics:par]{graphics::par()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
Renders an R-native diagnostic surface corresponding to the four QC
artifacts produced by the original Python workflow. Read retention and its
primer-match proxy use extraction provenance from \code{y}. Sequence lengths are
shown as read-weighted, per-round fractions so rounds with different depths
remain comparable. The diversity view contains unique counts, Shannon
entropy, and top-100 coverage.
}
\details{
When \code{y} is omitted, summaries already retained in \code{x} are still plotted.
Panels that require raw counts or extraction provenance state that the data
are unavailable instead of reconstructing them from incomplete summaries.
}
\examples{
p5 <- 'GGTAATACGACTCACTATAGGG'
p3 <- 'CCATGCATGCATGCATGCAT'
pools <- list(round_00 = rep(paste0(p5, 'ACGTACGT', p3), 500))
result <- run_selexprep(pools, low_total_reads = 0)
plot(S4Vectors::metadata(result)$qc, result, which = 'diversity')
}
