% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ena.R
\name{fetch_selexprep_reads}
\alias{fetch_selexprep_reads}
\title{Fetch FASTQ files from an ENA inspection}
\usage{
fetch_selexprep_reads(
  accession_or_inspection,
  outdir,
  dry_run = TRUE,
  overwrite = FALSE,
  timeout_seconds = 30,
  require_assigned_rounds = TRUE,
  overrides = numeric()
)
}
\arguments{
\item{accession_or_inspection}{A character accession or a
\code{selexprep_inspection} returned by \code{inspect_selexprep_accession()}.}

\item{outdir}{Destination directory for downloaded FASTQ files.}

\item{dry_run}{Whether to return the plan without downloading.}

\item{overwrite}{Whether an existing destination file may be replaced.}

\item{timeout_seconds}{Positive timeout for metadata and FASTQ requests.}

\item{require_assigned_rounds}{Whether to refuse ambiguous or missing round
assignments before creating a download plan.}

\item{overrides}{Optional named integer mapping of run accessions to manually
curated round numbers.}
}
\value{
A \code{selexprep_fetch_result} list containing the run-level \verb{$plan},
\verb{$downloaded_files}, and \verb{$dry_run}.
}
\description{
Creates a deterministic, checksum-aware download plan from an existing
inspection or an accession. By default no bytes are transferred; set
\code{dry_run = FALSE} to download to \code{outdir}. Each completed file is checked
against ENA's MD5 value when one is supplied.
}
\examples{
\donttest{
inspection <- inspect_selexprep_accession('PRJDB19098')
fetch_selexprep_reads(inspection, 'raw/PRJDB19098')
}
}
