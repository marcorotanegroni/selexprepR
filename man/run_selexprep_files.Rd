% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{run_selexprep_files}
\alias{run_selexprep_files}
\title{Run selexprepR directly from FASTQ files}
\usage{
run_selexprep_files(
  x,
  round = NULL,
  mate = NULL,
  max_reads_per_file = NULL,
  ...
)
}
\arguments{
\item{x}{A character vector of FASTQ paths, a data frame-like object with
\code{path} and \code{round_number} columns, or a non-dry-run
\code{selexprep_fetch_result} returned by \code{\link[=fetch_selexprep_reads]{fetch_selexprep_reads()}}.}

\item{round}{Optional vector of non-negative round numbers. It is required
for character paths unless their vector names end in round numbers.}

\item{mate}{Optional vector containing \code{R1}/\code{R2} or \code{1}/\code{2}. When omitted,
mate labels are inferred from filenames.}

\item{max_reads_per_file}{Optional positive maximum number of records read
from each FASTQ file.}

\item{...}{Named arguments forwarded to \code{\link[=run_selexprep]{run_selexprep()}}. The input
sequence, paired-mate, and read-source arguments are supplied by this
function and cannot be overridden.}
}
\value{
A \code{SummarizedExperiment} as returned by \code{\link[=run_selexprep]{run_selexprep()}}.
}
\description{
Convenience bridge combining \code{\link[=read_selexprep_inputs]{read_selexprep_inputs()}} and
\code{\link[=run_selexprep]{run_selexprep()}}. File-level provenance is attached to
\code{metadata(result)$input_files}.
}
\examples{
\dontrun{
experiment <- run_selexprep_files(
    c(round_00 = 'round_00.fastq.gz',
      round_01 = 'round_01.fastq.gz')
)
}
}
