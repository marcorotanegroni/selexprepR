% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demultiplex.R
\name{selexprep_demultiplex}
\alias{selexprep_demultiplex}
\title{Demultiplex SELEX reads with user-supplied barcodes}
\usage{
selexprep_demultiplex(
  sequences,
  barcodes,
  paired_mates = NULL,
  max_mismatches = 1L,
  trim_barcode = TRUE
)
}
\arguments{
\item{sequences}{R1 sequences as a character vector or
\code{Biostrings::XStringSet}.}

\item{barcodes}{Named integer vector mapping barcode sequences to
non-negative round numbers, for example \code{c(AAAAA = 0L, TTTTT = 1L)}.}

\item{paired_mates}{Optional R2 sequences in the same order as \code{sequences}.}

\item{max_mismatches}{Maximum Hamming distance allowed from a barcode.}

\item{trim_barcode}{Whether to remove the matched barcode from assigned R1
sequences.}
}
\value{
A \code{selexprep_demultiplex} object. Its first four fields follow the
\code{selexprep_read_inputs} contract and can be supplied directly to
\code{\link[=run_selexprep]{run_selexprep()}}. Additional fields contain unassigned reads, a summary,
and the validated barcode map.
}
\description{
Assigns reads to selection rounds from an equal-length barcode at the R1
5-prime end. Barcode sequences are supplied explicitly; they are never
inferred. R2 mates follow their R1 assignment and remain untrimmed.
}
\details{
Barcodes must have pairwise Hamming distance of at least
\code{2 * max_mismatches + 1}, preventing ambiguous assignments. RNA \code{U} and DNA
\code{T} are treated as equivalent during matching.
}
\examples{
reads <- c("AAAAACCCCC", "AAAATGGGGG", "TTTTTACGTA", "GGGGGAAAAA")
demultiplexed <- selexprep_demultiplex(
    reads,
    c(AAAAA = 0L, TTTTT = 1L)
)
demultiplexed$summary
}
