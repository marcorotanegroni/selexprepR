% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_selexprep_counts}
\alias{read_selexprep_counts}
\title{Read a selexprep count table}
\usage{
read_selexprep_counts(path)
}
\arguments{
\item{path}{Path to a \code{.tsv}, \code{.csv}, \code{.rds}, or \code{.parquet} count file.}
}
\value{
A validated \code{S4Vectors::DataFrame} with a
\code{Biostrings::BStringSet} sequence column.
}
\description{
Reads the stable four-column count contract (\code{sequence}, \code{reads}, \code{rank},
\code{rpm}) from TSV, CSV, RDS, or Parquet. Parquet support is optional and
requires \code{arrow}; TSV is the portable default for R-native workflows.
}
\examples{
path <- tempfile(fileext = ".tsv")
write_selexprep_counts(selexprep_count(c("ACGT", "ACGT", "GGGG")), path)
read_selexprep_counts(path)
}
