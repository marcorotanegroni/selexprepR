% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{read_selexprep_inputs}
\alias{read_selexprep_inputs}
\title{Read FASTQ files into round-aware pipeline inputs}
\usage{
read_selexprep_inputs(x, round = NULL, mate = NULL, max_reads_per_file = NULL)
}
\arguments{
\item{x}{A character vector of FASTQ paths, a data frame-like object with
\code{path} and \code{round_number} columns, or a non-dry-run
\code{selexprep_fetch_result} returned by \code{\link[=fetch_selexprep_reads]{fetch_selexprep_reads()}}.}

\item{round}{Optional vector of non-negative round numbers. It is required
for character paths unless their vector names end in round numbers.}

\item{mate}{Optional vector containing \code{R1}/\code{R2} or \code{1}/\code{2}. When omitted,
mate labels are inferred from filenames.}

\item{max_reads_per_file}{Optional positive maximum number of records read
from each FASTQ file.}
}
\value{
A \code{selexprep_read_inputs} list with \code{sequences_by_round}, optional
\code{paired_mate_streams}, \code{read_source}, and a file-level \code{DataFrame} named
\code{files}. The first three elements can be passed directly to
\code{\link[=run_selexprep]{run_selexprep()}}, or use \code{\link[=run_selexprep_files]{run_selexprep_files()}}.
}
\description{
Converts local FASTQ paths or a completed ENA fetch result into the R-native
inputs used by \code{\link[=run_selexprep]{run_selexprep()}}. Files belonging to the same round and mate
are concatenated in input-table order. Plain and gzip-compressed FASTQ files
are supported through \code{\link[=read_selexprep_fastq]{read_selexprep_fastq()}}.
}
\details{
Mate labels are inferred conservatively from conventional \verb{_R1}, \verb{_R2},
\verb{_1}, and \verb{_2} filename suffixes. Files without a mate suffix are treated as
single-end only when no R2 file is present. Paired inputs must have both
mates, with equal loaded read counts, in every round.
}
\examples{
path <- tempfile(fileext = '.fastq')
writeLines(c('@read_1', 'ACGT', '+', 'IIII'), path)
inputs <- read_selexprep_inputs(path, round = 0)
inputs$sequences_by_round
}
