% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastq.R
\name{read_selexprep_fastq}
\alias{read_selexprep_fastq}
\title{Read sequences from a FASTQ file}
\usage{
read_selexprep_fastq(path, max_reads = NULL)
}
\arguments{
\item{path}{Path to a FASTQ or FASTQ.gz file.}

\item{max_reads}{Optional positive maximum number of records to retain.}
}
\value{
A \code{Biostrings::BStringSet} of read sequences.
}
\description{
Reads a plain or gzip-compressed FASTQ file into a \code{Biostrings::BStringSet}.
\code{BStringSet} deliberately preserves both DNA and RNA alphabets, avoiding the
loss of uracil that would occur when every input is coerced to DNA.
}
\examples{
path <- tempfile(fileext = '.fastq')
writeLines(c('@read_1', 'ACGU', '+', 'IIII'), path)
read_selexprep_fastq(path)
}
