% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounds.R
\name{infer_selexprep_rounds}
\alias{infer_selexprep_rounds}
\title{Infer SELEX round assignments from public metadata}
\usage{
infer_selexprep_rounds(runs, overrides = numeric())
}
\arguments{
\item{runs}{A data frame or \code{S4Vectors::DataFrame} containing
\code{run_accession} and optional metadata columns \code{sample_title},
\code{library_name}, \code{experiment_title}, \code{design_description}, and
\code{sample_attributes}.}

\item{overrides}{Optional named integer vector mapping run accessions to
manually curated round numbers.}
}
\value{
A \code{S4Vectors::DataFrame} with one conservative round assignment per
run. \code{round_candidates} is a list-column and preserves ambiguity.
}
\description{
Applies a conservative, ordered metadata cascade: structured sample
attributes, sample title, library name, experiment title, and design
description. A run remains unsafe when no round is found or when one field
contains conflicting candidates. The function records evidence rather than
using enrichment signals to repair metadata.
}
\examples{
runs <- data.frame(
    run_accession = c("SRR1", "SRR2"),
    sample_title = c("Thrombin Round 1", "Thrombin Round 2")
)
infer_selexprep_rounds(runs)
}
