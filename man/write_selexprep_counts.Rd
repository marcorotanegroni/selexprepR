% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_selexprep_counts}
\alias{write_selexprep_counts}
\title{Write a selexprep count table}
\usage{
write_selexprep_counts(counts, path)
}
\arguments{
\item{counts}{A count table returned by \code{selexprep_count()}.}

\item{path}{Output path ending in \code{.tsv}, \code{.csv}, \code{.rds}, or \code{.parquet}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the stable four-column count contract. Use TSV for a dependency-free,
portable exchange format; Parquet is available when the optional \code{arrow}
package is installed.
}
\examples{
counts <- selexprep_count(c('ACGT', 'ACGT', 'GGGG'))
path <- tempfile(fileext = '.tsv')
write_selexprep_counts(counts, path)
}
