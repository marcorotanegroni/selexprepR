% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{build_selexprep_manifest}
\alias{build_selexprep_manifest}
\title{Build an R-native reproducibility manifest}
\usage{
build_selexprep_manifest(
  library_report,
  input_paths = character(),
  output_paths = character(),
  accession = NULL,
  bioproject_id = NULL,
  runs = character(),
  parameters = character(),
  runtime_seconds_per_stage = numeric(),
  flags = character(),
  input_root = NULL,
  output_root = NULL
)
}
\arguments{
\item{library_report}{A \code{selexprep_library_report}.}

\item{input_paths}{Existing input file paths to hash.}

\item{output_paths}{Existing output file paths to hash.}

\item{accession}{Optional public accession.}

\item{bioproject_id}{Optional BioProject accession.}

\item{runs}{Run accessions.}

\item{parameters}{Named character parameters used for the run.}

\item{runtime_seconds_per_stage}{Named numeric stage durations.}

\item{flags}{Character QC flag names.}

\item{input_root}{Optional root used to make input hash keys relative.}

\item{output_root}{Optional root used to make output hash keys relative.}
}
\value{
A validated \code{selexprep_manifest}.
}
\description{
Builds a versioned \code{selexprep_manifest_v2} object. The reader also accepts
the historical Python \code{selexprep_manifest_v1} schema, so existing analysis
records remain inspectable. Hashes are computed for every supplied existing
path and are keyed relative to \code{input_root} or \code{output_root} when supplied.
}
\examples{
p5 <- 'GGTAATACGACTCACTATAGGG'
p3 <- 'CCATGCATGCATGCATGCAT'
report <- selexprep_detect(list(round_00 = rep(paste0(p5, 'ACGTACGTACGTACGT', p3), 500)))
build_selexprep_manifest(report)
}
