% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_selexprep}
\alias{run_selexprep}
\title{Run the in-memory SELEX preprocessing workflow}
\usage{
run_selexprep(
  sequences_by_round,
  read_source = c("R1", "R2", "R1_AND_R2", "INTERLEAVED", "UNKNOWN"),
  paired_mate_streams = NULL,
  accession = NULL,
  sampling_seed = 42L,
  max_reads_per_round = NULL,
  low_total_reads = 10000L,
  primer_5p = NULL,
  primer_3p = NULL,
  extraction_mode = NULL
)
}
\arguments{
\item{sequences_by_round}{A named list of R1 pools, as accepted by
\code{selexprep_detect()}, or a \code{selexprep_read_inputs} object.}

\item{read_source}{Input read layout.}

\item{paired_mate_streams}{Optional named R2 pools.}

\item{accession}{Optional public-study accession stored in metadata.}

\item{sampling_seed}{Seed forwarded to \code{selexprep_detect()}.}

\item{max_reads_per_round}{Optional detection subsampling cap.}

\item{low_total_reads}{QC threshold for per-round read depth.}

\item{primer_5p}{Optional manually reviewed 5-prime primer override.}

\item{primer_3p}{Optional manually reviewed 3-prime primer override.}

\item{extraction_mode}{Optional manually reviewed extraction-mode override.}
}
\value{
A \code{SummarizedExperiment} with the LibraryReport, extraction and QC
result in its metadata.
}
\description{
Composes primer detection, extraction, sequence counting and quality control
into a single Bioconductor result. The primary output is a
\code{SummarizedExperiment} with one column per round and a sparse \code{counts}
assay. The LibraryReport, extraction result and QC report are retained in
\code{metadata()} for reproducibility and inspection.
}
\details{
Split-primer paired-end inputs return a valid zero-row experiment because
full inserts cannot be counted without read merging. Their separate trimmed
sides remain available in \code{metadata(result)$extraction}.
}
\examples{
p5 <- 'GGTAATACGACTCACTATAGGG'
p3 <- 'CCATGCATGCATGCATGCAT'
pools <- list(round_00 = rep(paste0(p5, 'ACGTACGTACGTACGT', p3), 500))
run_selexprep(pools, low_total_reads = 0)
}
